# Sieu tham so tot nhat tim duoc cho RandomForestClassifier tren split hien tai
n_estimators: 200
max_depth: 10
min_samples_split: 5
min_samples_leaf: 1
max_features: 0.5
class_weight: balanced_subsample
criterion: log_loss
bootstrap: false
